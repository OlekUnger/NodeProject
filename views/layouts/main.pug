doctype html
html
    head
        title!= title
        if showTests
            link(rel='stylesheet' href='/vendor/mocha.css')
        script(src='https://code.jquery.com/jquery-2.0.2.min.js')
    body
        header
            img(src='/img/big-logo.png' alt ='Логотип')
        h1!= message
        block content
        if showTests
            #mocha
                script(src='/vendor/mocha.js')
                script(src='/vendor/chai.js')
                script.
                    mocha.ui('tdd');
                    var assert = chai.assert;
                script(src='/qa/tests-global.js')
                if pageTestScript
                    script(src= pageTestScript)
                script.
                    mocha.run();